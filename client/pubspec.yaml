name: woosttogo
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^0.5.25
  search_map_place: ^0.1.1
  geolocator: ^5.3.0
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  fonts:
  - family: Roboto     
    fonts:
    - asset: build/flutter_assets/fonts/Roboto-Regular.ttf
  uses-material-design: true

